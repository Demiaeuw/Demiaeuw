-Installer WSL2

	-Dans PowerShell
		wsl --install
	-Vérifier la version
		wsl -l -v

-Installer Ubuntu

	-Dans PowerShell
		wsl --list --online
	-Installer une distro spécifique
		wsl --install -d <Nom_de_la_Distribution>

-Mise a jour WSL2

	-Dans PowerShell
		wsl --update

-Redemarrer WSL2

	-Dans PowerShell
		wsl --shutdown

-Redemarrer le pc quand il le demande

-Installation des dépendances necessaire au cursur 42

	-Dans WSL
		sudo apt update
		sudo apt upgrade

	-Interface graphique
		sudo apt install x11-apps -y

	(-Explorateur de fichier)
		sudo apt install nautilus -y

	-Norminette
		sudo apt install python3-pip
	|	pip3 install norminette
	 \	norminette -v
	  \
	    ->	-si WARNING
		vim ~/.bashrc
		-ajouter la ligne a la fin du fichier
		export PATH="$HOME/.local/bin:$PATH"

	-Valgrind
		sudo apt install valgrind
		valgrind --version 
	

-SSH key et git /  github

	-Config base de git
		git config user.name <votre_nom>
		git config user.email <votre_email@example.com>
		git config --global --list

	-Création clé SSH
		ssh-keygen
		enter
		enter
		enter
		enter
		cd
		cd .ssh
		cat id_rsa.pub
		-ajoutez cette clé ou c'est necessaire.

		ATTENTION, si vous avez deja une clé SSH pour votre environement Windows, vous devrez malgrès tout en generer 
		une pour WSL, pour eviter les erreurs lors de vos commit. Passer par la clé de votre windows créer des 
		erreur de sync pour utilisé plainement les fonctionalité de git.

-Utilisé VScode

	-Dans VScode
		-Installer l'addon Explorateur Distant
		-Connectez vous a votre distro Ubuntu via cet explorateur Distant

			Cela permet de lancer VScode directement dans l'environement Linux de WSL 
			pour eviter les problemes. Si vous ouvrez le dossier via l'explorateur Windows directement 
			cela peut generer des erreur de compilation et autre.

			Pour quitter WSL dans VScode cliquez en bas a gauche sur l'icone a double chevron pour acceder au menu
			des connection a distance.

-Accés a WSL via terminal

	-Je vous conseil de passer par l'apps terminal de windows et d'ouvrir un nouvel onglet en selectionant Ubuntu
	Vous pouvez egalement, dans PowerShell tape --> wsl ce qui vous connectera a votre WSL 